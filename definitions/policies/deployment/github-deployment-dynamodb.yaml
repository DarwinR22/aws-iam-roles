policy:
  name: "github-deployment-dynamodb"
  description: "Gestión completa de tablas DynamoDB sin restricciones ABAC para CI/CD deployment"
  service: "dynamodb"
  version: "1.0.0"
  
  statements:
    - sid: "DynamoDBTableManagement"
      effect: "Allow"
      actions:
        - "dynamodb:CreateTable"
        - "dynamodb:DeleteTable"
        - "dynamodb:DescribeTable"
        - "dynamodb:UpdateTable"
        - "dynamodb:ListTables"
        - "dynamodb:DescribeTimeToLive"
        - "dynamodb:UpdateTimeToLive"
        - "dynamodb:ListTagsOfResource"
        - "dynamodb:TagResource"
        - "dynamodb:UntagResource"
        - "dynamodb:DescribeContinuousBackups"
        - "dynamodb:UpdateContinuousBackups"
        - "dynamodb:DescribeKinesisStreamingDestination"
        
      resources:
        - "arn:aws:dynamodb:*:*:table/*"

    - sid: "DynamoDBDataAccess"
      effect: "Allow"
      actions:
        - "dynamodb:GetItem"
        - "dynamodb:PutItem"
        - "dynamodb:DeleteItem"
        - "dynamodb:UpdateItem"
        - "dynamodb:Query"
        - "dynamodb:Scan"
        - "dynamodb:BatchGetItem"
        - "dynamodb:BatchWriteItem"
        
      resources:
        - "arn:aws:dynamodb:*:*:table/*"

    - sid: "DynamoDBBackupManagement"
      effect: "Allow"
      actions:
        - "dynamodb:CreateBackup"
        - "dynamodb:DeleteBackup"
        - "dynamodb:DescribeBackup"
        - "dynamodb:ListBackups"
        - "dynamodb:RestoreTableFromBackup"
        - "dynamodb:RestoreTableToPointInTime"
      resources:
        - "arn:aws:dynamodb:*:*:table/*/backup/*"

    - sid: "DynamoDBStreamAccess"
      effect: "Allow"
      actions:
        - "dynamodb:DescribeStream"
        - "dynamodb:GetRecords"
        - "dynamodb:GetShardIterator"
        - "dynamodb:ListStreams"
      resources:
        - "arn:aws:dynamodb:*:*:table/*/stream/*"

# Metadata
environments: ["dev", "qa", "prod"]
owner: "Infrastructure Team"

tags:
  Service: "DynamoDB"
  AccessLevel: "FullManagement"
  PolicyType: "ABAC"
  ManagedBy: "mci-aws-iam"
